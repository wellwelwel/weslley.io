/* Shared with the downloads tool, which draws the curve at build time. */

export const WIDTH = 300;
export const HEIGHT = 100;

/** Headroom kept above the busiest sample. */
export const INSET = 4;
